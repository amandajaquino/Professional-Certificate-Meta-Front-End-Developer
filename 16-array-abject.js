/* //In JS array are objects, so it have some properties and methods
//push method: add new item
var fruits = [];
fruits.push("apple");
fruits.push('pear');
console.log(fruits);
// pop method: remove last item
fruits.pop();
console.log(fruits); */

// build a function 

/* function arrayBuilder(one, two, three) {
    var arr = [];
    arr.push(one);
    arr.push(two);
    arr.push(three);
    console.log(arr);
}

// call the arrayBuilder

arrayBuilder('apple', 'pear', 'plum'); */

//using return instead of console

/* function arrayBuilder(one, two, three) {
    var arr = [];
    arr.push(one);
    arr.push(two);
    arr.push(three);
    return arr;
}

// call to a variable
var simpleArr = arrayBuilder('apple', 'pear', 'plum');
console.log(simpleArr) */
