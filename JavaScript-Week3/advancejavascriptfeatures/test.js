/* const meal = ["soup", "steak", "ice cream"]
    let [starter] = meal;
    console.log(starter);*/


/*     let food = "Chocolate";
    console.log(`My favourite food is ${food}`); */

  /*   let set = new Set();
    set.add(1);
    set.add(2);
    set.add(3);
    set.add(2);
    set.add(1);
    console.log(set); */

    /* let obj = {
        key: 1,
        value: 4
    };

    let output = { ...obj };
    output.value -= obj.key;

    console.log(output.value); */

   /*  function count(...basket) {
        console.log(basket.length)
    }

    count(10, 9, 8, 7, 6); */
/* 
    The answer is 5 because the count function takes in a rest parameter ...basket, which allows it to accept any number of arguments passed to the function.

In this case, the function is called with five arguments: 10, 9, 8, 7, 6. These arguments are passed to the count function and are stored in an array called basket.

The console.log(basket.length) statement in the function logs the length of the basket array, which is 5 in this case. Therefore, the output of the function will be 5, indicating the number of elements in the basket array */
